= form_with model: @post, local: true, class:"Form" do |f|
  = f.text_field :title, class: "Form__inputTitle input", placeholder: "タイトルを入力"
  = f.text_area :text, class: 'Form__inputText input', placeholder: "本文を入力"
  = f.file_field :image, class: 'Form__inputImage input'
  .Form__sendBtn
    = f.submit "投稿する", class: "btn"